{
  "name": "@dev/app",
  "version": "1.0.0",
  "license": "MIT",
  "babel": {
    "presets": [
      "@env"
    ]
  },
  "jest": {
    "preset": "@env"
  },
  "scripts": {
    "start": "node -r @babel/register src",
    "start:dev": "NODE_ENV=development nodemon --exec babel-node src",
    "debug": "NODE_ENV=development nodemon --inspect src",
    "test": "jest"
  },
  "dependencies": {
    "convict": "^6.0.0",
    "express": "^4.17.1",
    "http-proxy": "^1.18.1"
  },
  "devDependencies": {
    "@babel/node": "^7.10.5",
    "@env/babel-preset": "^1.0.0",
    "babel-jest": "^26.1.0",
    "babel-plugin-transform-glob-import": "^1.0.1",
    "jest": "^26.4.2",
    "nodemon": "^2.0.4",
    "react-refresh": "^0.8.3",
    "typescript": "^4.0.3"
  }
}
